import React from 'react';

const StatusBar = () => {
    return (
        <div className="status-bar">
            {/* Status information or quick links */}
        </div>
    );
}

export default StatusBar;
